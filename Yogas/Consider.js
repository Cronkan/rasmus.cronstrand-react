import React, { Component } from 'react'
import { withStyles, Typography } from '@material-ui/core';
const styles = theme => ({

});
class Consider extends Component {
    render() {
        return (
            <>
                <Typography variant="subtitle1">Consider</Typography>
            </>
        )
    }
}

export default withStyles(styles)(Consider);